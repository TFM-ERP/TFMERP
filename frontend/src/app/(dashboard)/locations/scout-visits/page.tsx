'use client';

import MasterScoutVisitsPanel from '@/components/production/MasterScoutVisitsPanel';

export default function MasterScoutVisitsPage() {
  return (
    <div className="max-w-5xl mx-auto px-4 py-6">
      <MasterScoutVisitsPanel />
    </div>
  );
}
