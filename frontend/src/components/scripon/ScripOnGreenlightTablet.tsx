'use client';
import React from 'react';
import { T_CSS, SEG5 } from './deviceCss';
import type { SxComp, SxFcast, SxRoi, SxDecision } from './ScripOnGreenlight';
import { useLocale } from '@/lib/i18n';
const SC = `.dvt .modes{display:flex;gap:7px}.dvt .mgrid{flex:1;display:grid;grid-template-columns:1fr 1fr;gap:13px;min-height:0}.dvt .col{display:flex;flex-direction:column;gap:13px;min-height:0}.dvt .scrl{flex:1;overflow:auto;display:flex;flex-direction:column;gap:13px}.dvt .comp{display:flex;align-items:center;gap:9px;padding:9px 0;border-top:1px solid var(--hair)}.dvt .comp:first-of-type{border-top:none}.dvt .cname{flex:1;font-size:12px;font-weight:600;color:var(--cream)}.dvt .cbar{width:64px;height:5px;border-radius:4px;background:#23262e;overflow:hidden}.dvt .cbar i{display:block;height:100%;background:linear-gradient(90deg,var(--gold),var(--gold2))}.dvt .cgr{font-size:11px;color:var(--mute)}.dvt .fwin{display:flex;align-items:center;gap:9px;padding:8px 0;border-top:1px solid var(--hair);font-size:12px}.dvt .fwin:first-of-type{border-top:none}.dvt .fl{width:74px;color:var(--faint);font-weight:600}.dvt .fb{flex:1;height:7px;border-radius:5px;background:#23262e;overflow:hidden}.dvt .fb i{display:block;height:100%;border-radius:5px}.dvt .fv{width:54px;text-align:end;font-weight:700;color:var(--cream)}.dvt .ring{width:104px;height:104px;border-radius:50%;display:grid;place-items:center;position:relative;margin:2px auto}.dvt .ring:before{content:"";position:absolute;inset:10px;border-radius:50%;background:var(--panel)}.dvt .rv{position:relative;font-size:25px;font-weight:800;color:var(--cream)}.dvt .rs{position:relative;font-size:9px;color:var(--faint)}.dvt .roi{width:100%;border-collapse:collapse}.dvt .roi th,.dvt .roi td{text-align:end;padding:7px 5px;font-size:11.5px;border-bottom:1px solid var(--hair)}.dvt .roi th{color:var(--faint);font-weight:700;font-size:9.5px}.dvt .roi td:first-child,.dvt .roi th:first-child{text-align:start;color:var(--cream);font-weight:600}.dvt .quad{display:grid;grid-template-columns:1fr 1fr;gap:12px}.dvt .qc{background:var(--panel);border:1px solid var(--hair);border-radius:13px;padding:14px;display:flex;flex-direction:column;gap:7px}.dvt .ql{font-size:11px;font-weight:700;letter-spacing:.5px;color:var(--gold)}.dvt .qa2{font-size:12px;color:var(--text);line-height:1.45}.dvt .qmeter{height:5px;border-radius:4px;background:#23262e;overflow:hidden}.dvt .qmeter i{display:block;height:100%;background:linear-gradient(90deg,var(--gold),var(--gold2))}.dvt .sc{display:flex;align-items:center;gap:10px;padding:9px 0;border-top:1px solid var(--hair)}.dvt .sc:first-of-type{border-top:none}.dvt .scn{width:110px;font-size:12px;font-weight:600;color:var(--cream)}.dvt .scbar{flex:1;height:6px;border-radius:5px;background:#23262e;overflow:hidden}.dvt .scbar i{display:block;height:100%;background:linear-gradient(90deg,var(--gold),var(--gold2))}.dvt .scv{width:40px;text-align:end;font-size:12px;font-weight:700;color:var(--cream)}.dvt .costrow{display:flex;align-items:center;gap:10px;padding:10px 0;border-top:1px solid var(--hair)}.dvt .costrow:first-of-type{border-top:none}.dvt .ci{flex:1}.dvt .cii{font-size:12px;font-weight:600;color:var(--cream)}.dvt .cin{font-size:10.5px;color:var(--faint)}.dvt .cs{font-size:13px;font-weight:800;color:var(--green)}.dvt .verdict{display:inline-flex;align-items:center;padding:6px 13px;border-radius:999px;font-size:12px;font-weight:800;letter-spacing:.5px}.dvt .memo{font-size:12.5px;line-height:1.6;color:var(--text)}`;
const appealPct = (t: string) => { const u = String(t || '').toLowerCase(); if (u.includes('very high') || u.startsWith('high')) return 88; if (u.includes('med-high')) return 72; if (u.includes('med')) return 56; if (u.includes('low-med')) return 40; if (u.includes('low')) return 26; return 50; };
const vColor = (v: string) => { const u = String(v || '').toUpperCase(); if (u.includes('GREEN') || u === 'GO') return 'var(--green)'; if (u.includes('PASS') || u.includes('HOLD')) return 'var(--red)'; return 'var(--amber)'; };
type P = { title: string; meta: string; mode: string; onTab: (k: string) => void; comps: SxComp[]; forecast: SxFcast[]; prob: { pct: number; verdict: string; note: string }; roi: SxRoi[]; prescription: string; decision: SxDecision; onNav: (k: string) => void; onBack: () => void; onAction: (k: string) => void; toast?: string | null };
export default function ScripOnGreenlightTablet(p: P) {
  const { dir, t } = useLocale();
  const ring = `conic-gradient(var(--gold) 0 ${p.prob.pct}%,#23262e ${p.prob.pct}% 100%)`; const m = p.mode; const dec = p.decision;
  return (<div className="dvt" dir={dir}><style dangerouslySetInnerHTML={{ __html: T_CSS + SC }} />
    <div className="top"><div className="tl"><div className="logo" onClick={p.onBack}>TFM</div><div className="proj">{t('Greenlight')}</div></div>
      <div className="seg">{SEG5.map(([k, l, d]) => <button key={k} className={'segtab' + (k === 'greenlight' ? ' on' : '')} onClick={() => p.onNav(k)}><svg className="ico" viewBox="0 0 24 24"><path d={d} /></svg>{t(l)}</button>)}</div>
      <button className="btn gold" onClick={() => p.onAction('forecast')}><svg className="ico" viewBox="0 0 24 24" style={{ stroke: '#1a1509' }}><path d="M3 3v18h18M7 14l3-3 3 3 5-6" /></svg>{t('Forecast')}</button></div>
    <div className="content"><div><div className="h1">{t('Market & decision')}</div><div className="sub">{t('Probabilistic, with confidence bands — never a guarantee.')}</div></div>
      <div className="modes"><button className={'chip' + (m === 'market' ? ' on' : '')} onClick={() => p.onTab('market')}>{t('Market')}</button><button className={'chip' + (m === 'audience' ? ' on' : '')} onClick={() => p.onTab('audience')}>{t('Audience')}</button><button className={'chip' + (m === 'cost' ? ' on' : '')} onClick={() => p.onTab('cost')}>{t('Cost')}</button><button className={'chip' + (m === 'decision' ? ' on' : '')} onClick={() => p.onTab('decision')}>{t('Decision')}</button></div>
      {m === 'market' && <div className="mgrid"><div className="col"><div className="card"><div className="pc-h"><span className="t">{t('Comparables')}</span><span className="eyebrow">{t('SIM · GROSS')}</span></div>{p.comps.map((c, i) => <div className="comp" key={i}><span className="cname">{c.name}</span><span className="cbar"><i style={{ width: c.sim + '%' }} /></span><span className="cgr">{c.sim}% · {c.gross}</span></div>)}</div>
        <div className="card" style={{ flex: 1 }}><div className="pc-h"><span className="t">{t('Forecast')}</span><span className="eyebrow">{t('P50')}</span></div>{p.forecast.map((f, i) => <div className="fwin" key={i}><span className="fl">{t(f.label)}</span><span className="fb"><i style={{ width: f.pct + '%', background: f.color }} /></span><span className="fv" style={f.gold ? { color: 'var(--gold2)' } : {}}>{f.value}</span></div>)}</div></div>
        <div className="col"><div className="card"><div className="pc-h"><span className="t">{t('Greenlight')}</span><span className="badge amber">{p.prob.verdict}</span></div><div className="ring" style={{ background: ring }}><span className="rv">{p.prob.pct}%</span><span className="rs">{t('P(greenlight)')}</span></div></div>
          <div className="card" style={{ flex: 1 }}><div className="pc-h"><span className="t">{t('Re-cost')}</span></div><table className="roi"><thead><tr><th>{t('Case')}</th><th>{t('Rev')}</th><th>{t('Margin')}</th><th>{t('ROI')}</th></tr></thead><tbody>{p.roi.map((r, i) => <tr key={i}><td style={r.tone ? { color: r.tone } : {}}>{t(r.case)}</td><td>{r.rev}</td><td style={r.tone ? { color: r.tone } : {}}>{r.margin}</td><td>{r.roi}</td></tr>)}</tbody></table></div></div></div>}
      {m === 'audience' && <div className="scrl"><div className="quad">{dec.audience.map((q, i) => <div className="qc" key={i}><div className="ql">{t(q.quadrant)}</div><div className="qa2">{t(q.appeal)}</div><div className="qmeter"><i style={{ width: appealPct(q.appeal) + '%' }} /></div></div>)}</div></div>}
      {m === 'cost' && <div className="scrl"><div className="card"><div className="pc-h"><span className="t">{t('Cost opportunities')}</span><span className="eyebrow">{t('ON THE NEGATIVE')}</span></div>{dec.costOps.map((c, i) => <div className="costrow" key={i}><div className="ci"><div className="cii">{t(c.item)}</div>{c.note && <div className="cin">{t(c.note)}</div>}</div><div className="cs">{c.saving}</div></div>)}</div></div>}
      {m === 'decision' && <div className="scrl"><div className="card"><div className="pc-h"><span className="t">{t('Scorecard')}</span><span className="eyebrow">{t('WEIGHTED')}</span></div>{dec.scorecard.map((s, i) => <div className="sc" key={i}><span className="scn">{t(s.criterion)}</span><span className="scbar"><i style={{ width: (Number(s.score) || 0) * 10 + '%' }} /></span><span className="scv">{s.score}/10</span></div>)}</div>
        <div className="card"><div className="pc-h"><span className="t">{t('Verdict')}</span><span className="badge amber">{dec.probability}% P</span></div><div style={{ display: 'flex', justifyContent: 'center', padding: '4px 0' }}><span className="verdict" style={{ color: vColor(dec.verdict), background: 'rgba(224,162,59,.16)' }}>{dec.verdict}</span></div><div className="memo">{t(dec.memo)}</div></div></div>}
    </div>{p.toast && <div className="toast">{p.toast}</div>}</div>);
}
