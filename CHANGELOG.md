# TFM System — Changelog

## V1.1 — 2026-06-04 (current)

Baseline for the **Enhancement** phase. Everything below is implemented and documented.

### Enhancement phase (in progress)
- **Crew directory — ADFC reel-scout field parity + paste-a-profile importer + per-department view:** studied the Abu Dhabi Film Commission industry directory (reel-scout) crew profile structure and brought your own directory to parity. `CrewMember` gains `resumeUrl` (CV), `skills` (Special Skills & Experience), `links` (Weblinks — IMDB/showreel/website/socials), `credits` (previous work — year/title/role), `categories` (additional dept-role), `affiliations` (experience tags: Emirati Crew & Talent, Arabic/Indian/International/TVC/Unscripted). Crew form gains a **Professional profile** card (repeatable links & credits, skills, resume upload, affiliation chips, extra roles) and a **"Paste profile"** importer — paste a CV/profile text and Anthropic extracts the fields for review (suggestion-only, never auto-saved; intended for your own roster / consented crew, **not** bulk scraping). Crew Directory gains a **By department** view grouping crew under their department with roles, clickable weblinks and a credits preview. Field mapping: ADFC "Description"→`bio`, "Weblinks"→`links`, "Credits"→`credits`, "Special Skills"→`skills`, "View File/Resume"→`resumeUrl`, multi "Categories"→`department`/`role`+`categories`. (Did **not** bulk-copy ADFC's directory — third-party personal data + their terms.) Endpoint: `POST /crew/parse-profile`
- **SYS-07 slice 8 — compliance expiry alerts + email (.msg) intake:** (1) **Expiry digest** — `GET /locations-library/expiring?days=N` scans every location document (NOC/insurance/…) and permit across master + project scopes for expired or soon-to-expire (≤N days) items; the Library page shows a collapsible **compliance-alerts banner** (expired in red, expiring in amber, with days-left, location and project). (2) **Email intake** — a dependency-free **CFBF/OLE2 `.msg` parser** (`msg-parser.ts`) extracts the subject, sender and **attachments** from an uploaded Outlook email; `POST …/import-email` (project) and `…/master/:id/import-email` (standalone) file the email plus each attachment into the document vault, auto-categorised (NOC/agreement/insurance/permit/quote/ID…) — inline signature images skipped. "Import email (.msg)" button in both the project Assess Documents tab and the standalone library Operations → Documents tab. Turns the `C:\Islam emails` archive into filed location records. Architecture: `docs/system/07` §8b/§9 (the two remaining ◐ items now ✅)
- **SYS-07 slice 7 — typed permits + authority directory, security/marshals, payment schedule, HSE; module made standalone (dual-target):** driven by the full mailbox study (permits = ~88% of the LM's work; security/guards the 2nd-biggest topic). **Permits** are now typed (`LocationPermitType`: Ground filming/Drone-GCAA/Road-RTA/Police/Airport/Heritage-DCT/Parking/Marine/Pyro/Firearms) with a reusable **`PermitAuthority` directory** (seeded: twofour54, GCAA, ITC/RTA, ADP, ADAC, DCT, municipalities) selectable on each permit. New **`LocationSecurity`** (guard company, headcount, marshals, shifts, rate→cost→ledger) and **`LocationPayment`** schedule (quote→deposit→balance→settled with a summary; pay posts to the ledger). **`METHOD_STATEMENT`** added to the document categories (HSE pack with risk assessment). **Standalone module:** `LocationPermit`/`LocationDocument`/`LocationSecurity`/`LocationPayment` are now **dual-target** — attachable to a master library location (no project) *or* a project location; new shared `LocationOpsService` + master endpoints under `/locations-library/:id/...`, while per-project endpoints stay under `/production/locations/...` (project scope posts to the ledger). UI: project Assess modal gains **Security** + **Payments** tabs and a typed-permit + authority picker; the Library detail drawer gains an **Operations (standalone)** panel with the same Security/Payments. Seed: `prisma/seed-permit-authorities.js`; managed from a new **Locations → Permit Authorities** screen (add/edit/delete issuing bodies, feeds every permit's authority picker). Architecture: `docs/system/07` §8b + §9
- **SYS-07 slice 6 — LM document vault & compliance (modelled on the real location-manager mailbox):** studying a location manager's 3.3 GB mailbox showed his day is largely document lifecycle work — **NOCs** dominate (bilingual EN/AR No Objection Certificates — by far the most frequent artifact), then location **agreements/releases**, **insurance** (PLL), **location guides**, **risk-assessment questionnaires**, IDs and rental quotes, exchanged with twofour54, Aldar, Masdar, Abu Dhabi Police, the Film Commission, SZGMC, Jubail Mangrove Park and production clients. Built to match: `LocationDocument` typed vault (category NOC/AGREEMENT/RELEASE/INSURANCE/GUIDE/RISK/ID/QUOTE/PERMIT_DOC, status DRAFT→REQUESTED→RECEIVED→SIGNED/ISSUED→EXPIRED, language EN/AR/BILINGUAL, party/authority/ref, issue/sign/**expiry** dates, amount, file + OCR) per project location; a **bilingual NOC letter generator** (server renders a print-ready EN+Arabic NOC from the location/production data); a **compliance gate** (signed agreement + valid non-expired insurance + NOC → "clear to confirm", with expiring/expired alerts); and a **LM status pipeline** on each location (Sourcing → NOC requested → Agreement sent → Permit applied → Insurance received → Confirmed → Wrapped). UI: a **Documents** tab + stage selector + compliance badges in the location Assess modal. Endpoints under `/production/locations` (documents CRUD + upload/OCR, compliance, stage, noc). Also produced a **contact directory** (`TFM Location Contacts (from LM mailbox).xlsx`) — 38 de-duplicated client/venue/authority contacts (company inferred from domain; names derived; phones left for manual fill as they live in Outlook contact fields). Architecture: `docs/system/07`
- **SYS-07 demo data (Abu Dhabi):** `prisma/seed-locations-demo.js` — 8 real Abu Dhabi master locations with accurate GPS, the real permit authority (Abu Dhabi Film Commission / twofour54 / UAE Media Council), day-rate guidance, restrictions, safety/hospital info and photos (Sheikh Zayed Grand Mosque, Louvre Abu Dhabi, Emirates Palace, Qasr Al Watan, Yas Marina Circuit, Qasr Al Hosn, Jebel Hafeet, Liwa/Moreeb Dune). Also seeds 3 scout assignments + 2 field candidates on the newest active projects, links 4 locations into projects (scenes/shoot dates), and adds a sample evaluation, permit (APPLIED) and risk register on the confirmed desert location. Idempotent (master upsert by code; everything else create-if-absent). Photos point at Wikimedia Commons; all location image surfaces (`/locations` card + drawer, map list, pack) now degrade to a seeded placeholder via `onError` if a file name has changed
- **Film Scout & Location Management (SYS-07) — follow-ups (offline · permit workflow · GIS):** (1) **Offline scout capture** — the field-submission form now persists to IndexedDB when offline via the existing sync engine (`scoutSubmission` queue kind in `useOfflineSync` + `offline-db`), auto-replays on reconnect (idempotent `clientId`), with an OfflineSyncBar and a "Save offline" button on the Scouting page. (2) **Permits routed through the universal approval engine** — "Submit for approval" on a permit calls `WorkflowService.start({ entityType:'LOCATION' })`; new seeded `LOCATION_PERMIT_STANDARD` chain (Location Manager → UPM → Producer); `applyCompletionEffect` gains a LOCATION branch that flips the `LocationPermit` to APPROVED/REJECTED on completion; permit moves to IN_REVIEW on submit and shows in My Approvals (`/production/locations/permits/:id/submit` + `/workflow`). (3) **Advanced GIS on the map** — Google **marker clustering** (@googlemaps/markerclusterer from CDN) + a usage-weighted **heatmap** toggle (Google `visualization` library, weight = times-used); pins/clusters hide under the heat layer and restore on toggle. Architecture: `docs/system/07` (phases 1–5 + these three ✅; Mapbox provider remains optional ◐)
- **Film Scout & Location Management (SYS-07) — slice 5, Map · analytics · location pack (domain complete, phases 1–5):** interactive **Google map** at `/locations/map` plotting every GPS-tagged library location (hybrid imagery, info windows, focusable side list); the browser Maps key is served at request time from `backend/.env` (`GOOGLE_MAPS_API_KEY`) via `GET /locations-library/map-config` — never baked into the build; the page shows a clear "add the key" notice if absent (restrict the key by HTTP referrer in Google Cloud Console). Library **analytics** endpoint (all-time spend, country spread, top-used locations, mapped count) shown as a stat strip. Printable **Location Pack** at `/locations/pack/[projectId]` (`GET /location-assessment/pack/:projectId`) — a per-project location book assembling media, address/access/owner/fee, permits table, and risk register, with a print/save-as-PDF button; launched from the project Locations header. Architecture: `docs/system/07` now ✅ phases 1–5; remaining ◐ = offline PWA queue, permit workflow routing, Mapbox/advanced GIS layers
- **Film Scout & Location Management (SYS-07) — slice 4, Permits · risk · budgeting:** `LocationPermit` lifecycle (type, authority, jurisdiction, ref #, status NOT_REQUIRED→APPLIED→IN_REVIEW→APPROVED/REJECTED/EXPIRED, application/approval/expiry dates, fee, conditions, attached doc) with **AI OCR intake** — a self-contained Anthropic-vision PERMIT prompt extracts authority/dates/fee/conditions into the form for review (suggestion-only, never auto-saved; needs `ANTHROPIC_API_KEY`). `LocationRisk` register: category, likelihood × impact = computed risk score (1-25, colour-banded), mitigation, owner, emergency/evacuation/nearest-medical. Location costs — the existing fee post plus a new `postCost` (used by "post permit fee to budget") — book to the project ledger (category "Location") and now **accrete to the linked master library** via `recomputeHistory`, so the library's total-spent/last-used update automatically. New endpoints under `/production/locations` (permits, risks, post-cost, permits/ocr); `LocationsLibraryService` injected into the production module for accrual. UI: **Permits** and **Risk** tabs added to the location Assess modal (permit list + OCR scan + post-fee, risk matrix with live score). Architecture: `docs/system/07` (phase 4 ✅; permit workflow routing ◐)
- **Film Scout & Location Management (SYS-07) — slice 3, Tech recces + weighted scoring:** `TechRecce` (per project-location: date, conductor, attendees, summary, status) with per-department `RecceNote` (CAMERA/GRIP/ELECTRIC/SOUND/ART/CONSTRUCTION/TRANSPORT/SECURITY/STUNTS/SFX/VFX/LOCATIONS — risks, equipment needs, crew needs, access/power, safety, photos; one note per department, upserted). `LocationEvaluation` scores ten criteria 1-5 (visual, access, logistics, cost, safety, production value, permit complexity *[inverted]*, feasibility, comfort, schedule) → **server-computed weighted score** (0-5) + auto recommendation (RECOMMENDED ≥4 / ACCEPTABLE ≥2.5 / NOT_RECOMMENDED). New `AssessmentService`/`AssessmentController` (`/location-assessment/recces|evaluations|compare`). UI: per-location **Assess** modal (1-5 score buttons with live weighted preview + Tech-recce tab with department note editor) and a project header **Compare** button → ranked side-by-side table (trophy on the top-scored candidate). Architecture: `docs/system/07` (phase 3 ✅)
- **Film Scout & Location Management (SYS-07) — slice 2, Scouting:** `ScoutAssignment` (brief: project, scenes, type INITIAL/PHOTO/DIRECTOR/PRODUCER/TECH_RECCE/PERMIT/FINAL, priority, budget target, due date, assigned scout, status OPEN→SUBMITTED→COMPLETED) + `ScoutSubmission` (field candidate: GPS, photo URLs, owner contact, est. fee, evaluation JSON; idempotent `clientId` so the offline queue can replay safely). New `ScoutingService`/`ScoutingController` in the locations module (`/scouting/assignments…`, submission create/status/accept). **Scouting** page under the Locations module: assignments board (priority/type/status, candidate counts), assignment drawer to review field candidates with photos + map links, and one-click **Accept → promote into Master Library** (dedup) with optional **link to project** — closing the scout→library loop; plus Shortlist/Reject. Capture form has on-device GPS ("use my current location") and is idempotent for the upcoming offline portal. Architecture: `docs/system/07` (phase 2 ✅; full PWA service-worker queue still ◐)
- **Film Scout & Location Management (SYS-07) — slice 1, Master Library backbone:** new standalone top-level **Locations** module (sidebar peer of Rentals, piggybacks `production` permission). `MasterLocation` (permanent company asset — no projectId: geo, access, technical, restrictions, ownership, permit authority, safety, and *accreted production-history aggregates* timesUsed/totalSpentToDate/lastUsedAt) + `LocationMedia` gallery + `MasterLocationStatus`/`LocationMediaType` enums; existing project `Location` extended with `masterLocationId` link + per-project `scenes`/`shootStart`/`shootEnd`. New `LocationsLibraryModule` (CRUD, search/filter, stats, media add/set-primary/remove, **two-way integration**: `linkToProject` snapshots a library location into a project, `promoteFromProject` lifts a project location into the library with name+geo dedupe, `recomputeHistory` accretes costs). Library page at `/locations` (card grid, search, status filter, detail drawer with media + history + per-project usages, create/edit modal). Project Locations tab gains **"Link from Library"** picker + per-card **Library** badge / **"Save to Library"** promote action. Migration script `prisma/migrate-locations-to-library.js` promotes all existing project locations into the library (idempotent, dedup, history seeded; locked projects only gain the link). Maps (Mapbox/Google) deferred to phase 5. Architecture: `docs/system/07`
- **Period-lock hardening:** `assertOpen()` now also guards ledger transaction status changes (`setStatus`, e.g. DRAFT→APPROVED/PAID) and petty-cash deletion of posted SPEND ledger entries — closed months can no longer be mutated via these paths
- **Rate Approvals page:** added doc-16 §4 trigger buttons — *Refresh sources* (allow-listed fetch) and *AI research* (all agreements) run directly from the queue; both only file PENDING proposals. Result banner + cited source/publisher link on rate-change cards
- **V1.2 slice 3 — expanded OCR:** invoice extractor generalized to `extractDocFields(task)` with three vision prompts (INVOICE / PETTY_CASH_RECEIPT / DIGITAL_TIMESHEET); `POST …/floats/:floatId/upload-receipt` → DRAFT petty-cash spend, `POST …/timesheets/:projectId/upload` → timecard draft (fields aligned to the Timecard model). "Scan receipt" button in the Cash tab and "Scan timesheet" button in Accounting ▸ Payroll both AI-pre-fill their forms (confidence shown, review before recording). Same guardrails — suggestion-only, period-guarded, never auto-posts
- **Paid invoices register** (Accounting ▸ Paid invoices): every released payment with vendor, invoice number, PO ref, account, amount, who approved + who released it (SoD trail), and clickable links to the attached vendor invoice documents; date-range filter, running total, CSV export. The disbursements/audit view. `GET /production/ledger/paid/:projectId`
- **Invoice details captured at invoicing:** recording a PO payable now requires the vendor **invoice number** (manual prompt or OCR), with optional invoice date; the OCR-scanned invoice document is now linked to the *transaction* (not just the PO) so the payment-release gate finds it. Closes the loop so an invoiced PO can actually be paid once its details + document are present
- **Accounting internal controls (procure-to-pay):** segregation of duties on the workflow — submitter can't approve own item, no double-approval, and the **approver cannot release the payment**; manual COST→APPROVED disabled while an approval workflow is active (no bypassing the chain); **PAID can never be set by status change** — only the payment run, which requires APPROVED + invoice number + attached vendor-invoice document; three-way match (invoice ≤ PO remaining) already enforced; audit fields `approvedById` + `paidById` stored on every transaction for finance/ADFC audit reconstruction. Doc 18 §5.5 documents the control model
- **PO approval-state fixes + payment control:** `PurchaseOrderStatus` gains SUBMITTED + REJECTED (the rejection was silently failing — no enum value); submitting a PO moves it to SUBMITTED so the direct-approve/route buttons disappear while pending; rejection now actually flips the PO to REJECTED with a "Revise" action (→ back to DRAFT to resubmit). Payment run now BLOCKS paying a cost unless it's APPROVED AND carries an invoice number AND has a vendor invoice document attached — clear per-item reasons returned
- **Workflow → project invoices/expenses:** a DRAFT project cost in the Ledger gets a "Submit for approval" action → routes through the INVOICE (if it carries an invoice number) or EXPENSE chain → on final approval the transaction flips to APPROVED and counts as an actual in the cost report. Per-project AP approval, distinct from the company finance module
- **Workflow → entity wiring (first integration):** workflow completion now applies the real effect decoupled via Prisma (PO/transfer/overage/timecard status flips, no circular deps); PO "Route for approval" submits into the universal engine → approvers act in My Approvals → final approval commits the PO to APPROVED. `isApproved()` available for the rest
- **Workflow admin UI** (Setup ▸ Approval Workflows): view/create/edit approval chains — name, entity type, ordered steps with approver by project-role template or global role, reorder/add/remove, activate. Admins build custom sequential chains without code
- **Universal Workflow & Approval Engine (core):** new `WorkflowModule` — `WorkflowDefinition`/`WorkflowNode`/`WorkflowInstance`/`ApprovalAction` models; engine service with sequential routing, auto-skip nodes, per-node approver resolution (project PermissionTemplate key OR global role), `isApproved()` gate, `myPending()` inbox; `GET/PUT /workflow/definitions`, `POST /workflow/start`, approve/reject/cancel, `GET /workflow/my-pending`, `GET /workflow/entity/:type/:id` history. 6 seeded standard chains (PO, petty cash, timecard, transfer, overage, invoice). "My Approvals" inbox page in the Production menu. Schema supports SLA/escalation/delegation/auto for later modes
- **Production sidebar declutter:** sub-tab bar gains a lightweight divider (`Page.divider`) separating daily-work entry points (Dashboard, Projects, Crew, Email) from master data (Labor & Fringe Master, Rate Approvals); nav helpers (matchActive, ALL_PAGES, goModule, overflow count) all skip dividers
- **Domain architecture north star** (`docs/system/06`): platform mapped as 9 core engines + 13 business systems, current build tagged ✅/◐/🔶, 9-level permission model + universal workflow target. Project page tabs regrouped domain-aligned (Overview · Planning · Budget & Cost · Accounting · Workforce · Locations · Setup & Output) to fix crowding
- **V1.2 workforce lifecycle (foundation):** `lifecycleState()` computes Prep / Active / Wrapped / Undated live from each crew assignment's contract dates (no schema change); badge on the crew-assignments table + `GET …/crew/project/:projectId/workforce-status` summary (counts + roster)
- **V1.2 slice 2b — capability enforcement:** `assertProjectCapability()` (FAIL-OPEN — no project role → global RBAC unchanged, zero lockout risk) now gates the three core financial-authority actions: cost-report snapshot **lock**, budget-transfer **approval**, and PO **approval**. `GET /production/projects/:id/my-authority` exposes the caller's effective project authority for UI button-gating
- **V1.2 slice 2 — per-project authorization:** `PermissionTemplate` (6 seeded standard roles) + `ProjectRoleAssignment` layered on the global RBAC; `projectAuthority(projectId, userId)` resolver; endpoints for templates/team/assign/remove; Project Settings "Project team & access" card. Field-level security enforced server-side on the project crew list — Dept Head / Coordinator / Observer get day rates, IBAN and PII blanked per their template's `fieldLevelAccess`
- **V1.2 slice 1 — crew ↔ ERP identity separation:** `CrewMember.parentSystemUserId` (nullable → User); `GET /crew/parent-users`, `PUT /crew/:id/parent-user` link/unlink, `hasErpAccess()` rule helper (null link = portal/payroll only, zero ledger access). Crew form gains an "ERP system access" link card; directory gains an Access badge (ERP user / Portal). Temporary crew never become parent-system users
- **Dedicated production bank account (ADFC audit chain):** `BankAccount.projectId` + `LedgerBankAccount.projectId` (payment half + reconciliation half); `GET/PUT /production/projects/:id/bank` returns/links both with last-reconciliation status and per-program compliance (driven by `complianceRules.dedicatedBankAccount`); Project Settings gains the linking card with compliance banner; Abu Dhabi Rebate Tracker shows a hard red prerequisite block until the account is linked, green status line incl. last reconciliation when compliant
- **Incentive audit & eligibility rules** (researched per authority): `IncentiveProgram.complianceRules` Json added and seeded for all 10 programs — audit requirement + auditor rule + deadline + dedicated-bank-account + local-entity + qualifying-spend conditions (ADFC: pre-approved auditor, 180-day Audited Expenditure Statement, dedicated project bank; GA mandatory GDOR audit; CA independent-CPA AUP; CAVCO >500k; BFI accountant's report; Jordan CPA reports; QSPI TBD). Doc 18 §5.4 adds the project-entity TB / Audit Pack matrix and enforcement spec
- **E-invoicing schema (KSA + Jordan):** `InvoiceClassification` enum (B2B/B2C/B2G) + full ZATCA Phase 2 field block on ProjectTransaction AND PurchaseOrder — UUID, ICV counter, XML hash, PIH chain, cryptographic stamp + CSID, TLV QR, clearance status/timestamp (verified vs ZATCA technical guidelines) — plus JoFotara fields (submission UUID, ISTD QR, UBL 2.1 marker). Clearance/submit middleware remains spec (doc 18 §4)
- **Dark mode:** sun/moon toggle in the top utility bar (persisted per browser, applied post-hydration so SSR never mismatches); dark theme re-points the design-system CSS variables and remaps the common raw Tailwind utilities globally — print pages stay light
- **Crew rate card v2 + dual phones:** CrewMember gains `phone2` (international/second) and a split rate card — Shoot day/weekly + shared Prep/Wrap day/weekly (per currency); labour blocks now auto-build PREP and WRAP at the prep/wrap rate and SHOOT at the shoot rate, stage- and unit-aware autofill, project assignment prefills daily + weekly
- **Master CoA switched to native industry numbering** (decision after reconciliation): new projects seed the exact Movie Magic/AICP topsheet — 1100–1800 ATL, 2000–4800 BTL, 5000–5800 POST, 6300–6900 OTHER (~36 accounts, ~150 lines, fringe classifications carried over). Existing projects keep their codes (stored section tiers untouched). Tier fallback now 1/2–4/5/6+; distribution ledger shifted to 7000 P&A / 7510 Revenue / 8100 COGS / 9100 Corporate; fringe GL codes → 6600s; identity mapping seed updated; `seed-mm-mapping.js` now provides TFM_LEGACY (old→new) translation and retires MM_INDUSTRY rows
- **Industry MM CoA reconciliation** (doc 17, from "Master cost numbers.docx"): industry numbering loaded as `MM_INDUSTRY` CoaMappingTable source (46 cited translations — the two systems collide on 1400/2100/2200/2500/3300/4100, so identity rows stay separate); 11 missing accounts added to the Master CoA (1800 ATL T&L, 2655 Set Strike, 2955 Animals & Wranglers, 2975 Picture Vehicles, 2985 BTL T&L, 3050 Post Staff, 3700 Stock Footage, 4600 2nd Unit, 4650 Aerial & Marine, 4700 Re-shoots, 4750 Tests); BreakdownCategory gains CAMERA / ADDITIONAL_LABOR / MECHANICAL_FX / ANIMAL_WRANGLER wired through all import/export/AI/DOOD maps
- **Bulk archive** (`PATCH /production/projects/bulk-archive`, write-permission): batch status → ARCHIVED (reversible); projects list hides archived by default with a Show-archived toggle, card checkboxes for multi-select and an "Archive selected (n)" action. Fixed recurring hydration error: home-page time-of-day greeting now computes client-side only
- **Project deletion** (`DELETE /production/projects/:id`, write-permission): cascade-deletes the project with everything under it; projects holding a LOCKED baseline or posted ledger transactions are blocked by default (historical-projects rule) and need an explicit force confirmation. Projects list cards get a delete button gated by typing the project number
- **Calendar anchoring & DOOD call sheets:** LOCKING a budget anchors Principal Photography — shootEndDate derived from the locked version's `shoot_days` global on the project's shoot start anchor (never hardcoded); new `CalendarAnchoringService` (`GET …/calendar/:projectId`) maps strips to real dates rippling from the anchor with PREP/SHOOT/WRAP/STRIKE phase labels sourced from Globals staging (budget-globals fallback); new `GET …/callsheet-data/:projectId?date=` returns the day's scenes + DOOD-filtered requirements — only SW/W/WF/SWF elements, Holds and Drops strictly excluded
- **Universal dynamic DOOD** (`DoodCalculationService` + `UniversalDoodPanel`): Day-Out-of-Days for EVERY breakdown category (cast, props, vehicles, stunts…) computed live from ProductionStrip × BreakdownElement — never stored, so moving a scene shifts every category's dates automatically. Industry codes SW/W/WF/SWF + algorithmic H (hold) vs D (drop) with configurable drop-after threshold; CAST merges legacy scene-cast lists; per-row Work/Hold totals; replaces the cast-only DOOD table in the Schedule tab. "Refresh Globals" runs all categories and stages shoot days + per-category tallies into ProjectGlobalsStaging (`doodTallies` Json column added) — budgets untouched until staging is explicitly pushed
- **Budget "Detailed lines" toggle:** labour-block stages render as aligned sub-rows under their line (plain purple PREP/SHOOT/WRAP labels + thin rail, qty/unit/rate/amount in the table's own columns); main line shows "N stages" with subtotal/fringe/total; OFF = previous compact summary; choice remembered per browser. Crew badge no longer prefixed with "(Name)"
- **FX online fetch** (`POST /fx/refresh` + "Fetch online" button in Setup ▸ Currencies & FX): pulls live rates from two independent free sources (open.er-api.com + fawazahmed0 currency-api), keeps the AED-favourable quote per currency (higher AED per foreign unit — prudent for foreign-cost budgeting), pins USD to the CBUAE peg 3.6725, optional `FX_MARGIN_PCT` safety margin, updates default set (USD/EUR/GBP/SAR/CAD/QAR/JOD) plus any currencies already in the table
- **Crew Directory unification:** taxonomy normalization script (`normalize-crew-taxonomy.js`, preview/--apply, ~100 synonym rules → canonical filmCrew.ts departments/roles, report file); sort controls (name/department/role/country, asc/desc); create/edit form is picker-only for department & role (no free text — legacy values shown as "(legacy)"); rate card reduced to ONE unified day rate (AED or USD) per person — LaborBlockEditor now auto-builds prep/shoot/wrap stages all carrying that same rate
- **Budget tab layout:** sub-account code now sits in a left column on each line (mono, tooltip shows sub-title) replacing the full-width gray code rows; new "Account total for NNNN" row after each account, "TOTAL <SECTION>" row after each section (tinted with the section colour), and a closing Budget Totals card — Total Above-The-Line / Total Below-The-Line / combined / GRAND TOTAL — computed live from line items
- **KSA / Qatar / Jordan jurisdictions** (deep-researched June 2026, official + ≥2 trade sources per figure): geo nodes + cited IncentivePrograms — Film Saudi up to 60% (raised 15 May 2026; min SAR 750k, 5 shoot days, pre-approval mandatory), Film AlUla 40%→50% + 10% uplifts, Qatar QSPI up to 50% (launched Nov 2025, apps Q2 2026, MCQ SPV required, caps TBD), Jordan RFC 25–45% (May 2025; min $250k, cap $5.25M, +GST/customs/payroll exemptions ≈56% total) — plus JurisdictionTaxRules (KSA VAT 15% w/ non-resident refund notes, Qatar no-VAT + 5% WHT, Jordan GST 16% w/ RFC upfront exemption) and official domains in the AI allow-list
- **Filming country drives the system:** new-project form gets a mandatory "Filming Country" selector (defaults UAE) → `productionCountryId`; Incentives tab resolves applicable programs from the filming country (labor-wizard geo as legacy fallback); Abu Dhabi claim tracker now renders only for UAE projects, other countries get program-notes guidance pending dedicated trackers
- **PGA added to the Labor & Fringe master** (seed-labor.js): Producers Guild of America (GUILD, US) with PRODUCER classification — explicit 0% "non-CBA, no mandated P&H" rule for scripted (documented, not silently missing) + PHBP commercial-producer health (~9%, estimate) for TVC/MV/corporate; sources producersguild.org + phbp.org added to the AI refresh/research allow-list
- **Multi-jurisdiction & AI-context schema:** `ProductionProject.productionCountry` (mandatory → `GeoNode`); new `JurisdictionTaxRule` (per-geo VAT rate + recovery rules, effective-dated, cited); new `CoaMappingTable` (external/Movie Magic code → Master CoA); new `ProjectGlobalsStaging` (Prep/Shoot/Wrap days, crew count, EUR/AED + USD/AED — held in staging until explicitly pushed to a WORKING budget version)
- **AI-reviewed Movie Magic import** (two-step, approval-gated): `POST …/import/ai-preview` parses the MMB file + runs `aiMapBudgetLines` (zero writes) → `CoaMappingReviewTable` in Project Settings shows original line vs suggested account/VAT with confidence bars (red <70%) + manual-override dropdown from the Master CoA → *Confirm and Push to Budget* clones the active version via `cloneVersion` (LOCKED baselines untouched) and injects only the approved lines into the new WORKING copy. Legacy direct import endpoints unchanged
- **`aiMapBudgetLines`** (`movie-magic/ai-mapping.service.ts`, `POST /production/movie-magic/:projectId/ai-map-lines`): AI CoA-mapping suggestions for unmapped imported lines — DynamicContext JSON injected into the system prompt, forced tool-use for strict JSON, every returned code hard-validated against the enterprise CoA (hallucinated codes rejected), VAT treatment from jurisdiction rules. Suggestions only; nothing is written
- **`DynamicContextService`** (`production/context/`): read-only RAG context builder — projectId → jurisdiction tax rules (geo-tree fallback) + active CoA mappings → minified JSON / system-prompt block for Anthropic calls, so imports stop hallucinating account codes. Exported from `ProductionModule`

### Production & budgeting
- Expanded Master Chart of Accounts seeded on every project (ATL 1100–1700 · BTL 2100–2990 · POST 3100–3600 · OTHER 4100–4500, ~35 accounts / ~165 lines with fringe classifications) — docs 13, 14 §2b
- Optional distribution & corporate ledger 6000–9000 (P&A / Revenue / COGS / Corporate): creation checkbox + guarded post-creation injection; new txn kinds `SALES_REVENUE`, `CORPORATE_OVERHEAD` — doc 15
- Budget lifecycle state machine DRAFT→REVIEW (V1..Vn)→APPROVED→LOCKED→WORKING, role-gated, full audit log; dual-column Top Sheet comparison (Locked Baseline | Working | Variance) — doc 12
- LOCKED immutability + working-copy cloning; line provenance (origin + archived AI suggestions, MANUAL_OVERRIDE detection)
- Budget transfers (PENDING→APPROVED/REJECTED) + approved overages → one revised-budget formula across all six finance surfaces; period locks (assertOpen) everywhere money posts
- Classification dropdown from the project labor snapshot; crew-to-budget-line link with rate pull

### Integrations & intake
- Movie Magic import/export (XML/CSV/.sex) at creation and from Settings; legacy fringe capture (one profile per distinct %), 4th-level sub-details → stages, merge strategies NEW_VERSION / UPDATE_ACTIVE — docs 10, 15
- OCR PO invoice intake (Anthropic vision) → DRAFT transactions
- Vendor self-onboarding (time-limited public link → staging → approve into Supplier master); supplier-linked project vendors
- Visual drag-and-drop breakdown→budget mapping; AI script breakdown
- PWA offline petty cash + locations with sync queue
- Role-specific dashboards (Finance/LP · 2nd AD/Coordinator)

### Setup masters
- Labor & Fringe master (geo → bodies → agreements → classifications → versioned rate rules), allow-listed refresh + AI research → approval-gated proposals — docs 06, 16
- Incentives master + Abu Dhabi rebate claim tracker — doc 07
- Currencies & FX rates (manual reference table, base AED) — doc 16

### Rentals
- Maintenance tab grouping (PM, damage, maintenance, tires, parts, jobs); asset photos multi-upload (15) with drag & drop

### Documentation
- `docs/production/` 01–16: full technical set incl. master visual references (14 project-side, 16 setup-side)

---

## V1.0

Initial system: rentals, finance (invoices/quotations/VAT), HR, clients/suppliers, accounting (GL/journals), production module core (projects, budget, scheduling, breakdown, call sheets, costing, ledger, payroll, locations, per diem, credits, approvals), compliance, integrations, audit, executive dashboard.
